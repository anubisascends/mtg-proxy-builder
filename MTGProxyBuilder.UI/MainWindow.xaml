<Window x:Class="MTGProxyBuilder.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:MTGProxyBuilder.UI.Controls"
        xmlns:converters="clr-namespace:MTGProxyBuilder.UI.Converters"
        mc:Ignorable="d"
        Title="MTG Proxy Builder"
        Height="950" Width="1600" WindowStartupLocation="CenterScreen"
        Background="#1E1E1E">
    <Window.Resources>
        <DropShadowEffect x:Key="PageShadow" BlurRadius="20" ShadowDepth="4" Opacity="0.35"/>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibility"/>
        <converters:FilePathToImageConverter x:Key="FilePathToImage"/>
        <converters:NullToCollapsedConverter x:Key="NullToCollapsed"/>
        <converters:BoolToFaceTextConverter x:Key="BoolToFaceText"/>
        <converters:FileNameConverter x:Key="FileNameConverter"/>

        <!-- Toolbar icon button style -->
        <Style x:Key="ToolbarIconBtn" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#CCC"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="14"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3E3E42"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="#555"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Row 0: Menu bar -->
            <RowDefinition Height="Auto"/> <!-- Row 1: Icon toolbar -->
            <RowDefinition Height="Auto"/> <!-- Row 2: Project tab bar -->
            <RowDefinition Height="Auto"/> <!-- Row 3: Update banner -->
            <RowDefinition Height="*"/>    <!-- Row 4: Main content -->
            <RowDefinition Height="Auto"/> <!-- Row 5: Status bar -->
        </Grid.RowDefinitions>

        <!-- ==================== MENU BAR (Row 0) ==================== -->
        <Menu Grid.Row="0" Background="#252526" Foreground="#CCC">
            <!-- File -->
            <MenuItem Header="_File">
                <MenuItem Header="_New Project" Command="{Binding NewProjectCommand}" InputGestureText="Ctrl+N"/>
                <MenuItem Header="_Open Project" Command="{Binding OpenProjectCommand}" InputGestureText="Ctrl+O"/>
                <MenuItem Header="Open _Recent" x:Name="RecentFilesMenu"
                          ItemsSource="{Binding RecentFiles}"
                          Visibility="{Binding HasRecentFiles, Converter={StaticResource BoolToVisibility}}">
                    <MenuItem.ItemContainerStyle>
                        <Style TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
                            <Setter Property="Header" Value="{Binding Converter={StaticResource FileNameConverter}}"/>
                            <Setter Property="ToolTip" Value="{Binding}"/>
                            <EventSetter Event="Click" Handler="OnRecentMenuItemClick"/>
                        </Style>
                    </MenuItem.ItemContainerStyle>
                </MenuItem>
                <Separator/>
                <MenuItem Header="_Save" Command="{Binding ActiveProject.Inner.SaveProjectCommand}" InputGestureText="Ctrl+S"/>
                <MenuItem Header="Save _As" Command="{Binding ActiveProject.Inner.SaveProjectAsCommand}" InputGestureText="Ctrl+Shift+S"/>
                <Separator/>
                <MenuItem Header="_Export PDF" Command="{Binding ActiveProject.Inner.ExportPdfCommand}" InputGestureText="Ctrl+E"/>
                <MenuItem Header="Export SVG Cut Lines" Command="{Binding ActiveProject.Inner.ExportSvgCommand}"/>
                <Separator/>
                <MenuItem Header="E_xit" Click="OnExitClick"/>
            </MenuItem>
            <!-- Edit -->
            <MenuItem Header="_Edit">
                <MenuItem Header="_Undo" Command="{Binding ActiveProject.Inner.UndoCommand}" InputGestureText="Ctrl+Z"/>
                <MenuItem Header="_Redo" Command="{Binding ActiveProject.Inner.RedoCommand}" InputGestureText="Ctrl+Y"/>
                <Separator/>
                <MenuItem Header="Clear All Cards" Command="{Binding ActiveProject.Inner.ClearAllCardsCommand}"/>
            </MenuItem>
            <!-- Cards -->
            <MenuItem Header="_Cards">
                <MenuItem Header="Add from _File" Command="{Binding ActiveProject.Inner.AddCardFromFileCommand}"/>
                <MenuItem Header="Import _Deck" Command="{Binding ActiveProject.Inner.ImportDeckCommand}"/>
                <MenuItem Header="_Refresh Deck" Command="{Binding ActiveProject.Inner.RefreshDeckCommand}"/>
                <MenuItem Header="Import _MPCFill XML" Command="{Binding ActiveProject.Inner.ImportMpcFillXmlCommand}"/>
                <MenuItem Header="Import _Text List..." Command="{Binding ActiveProject.Inner.ImportTextListCommand}"/>
                <Separator/>
                <MenuItem Header="_Remove Selected" Command="{Binding ActiveProject.Inner.RemoveCardCommand}" InputGestureText="Del"/>
                <Separator/>
                <MenuItem Header="Refresh _Selected Card Data" Command="{Binding ActiveProject.Inner.RefreshSelectedCardDataCommand}"/>
                <MenuItem Header="Refresh _All Card Data" Command="{Binding ActiveProject.Inner.RefreshAllCardDataCommand}"/>
                <Separator/>
                <MenuItem Header="Select _Front Art..." Command="{Binding ActiveProject.Inner.BrowseFrontArtworkCommand}"/>
                <MenuItem Header="Select _Back Art..." Command="{Binding ActiveProject.Inner.BrowseBackArtworkCommand}"/>
                <MenuItem Header="Select Back for All Cards..." Command="{Binding ActiveProject.Inner.SelectBackArtForAllCommand}"/>
                <MenuItem Header="Select Back for Cards Without Back..." Command="{Binding ActiveProject.Inner.SelectBackArtForMissingCommand}"/>
                <MenuItem Header="Clear Back Art (non-DFC)" Command="{Binding ActiveProject.Inner.ClearBackArtFromNonDfcCommand}"/>
                <MenuItem Header="Clear All Back Art" Command="{Binding ActiveProject.Inner.ClearBackArtFromAllCommand}"/>
            </MenuItem>
            <!-- Tools -->
            <MenuItem Header="_Tools">
                <MenuItem Header="_Art Library" Command="{Binding ManageFrontArtLibraryCommand}"/>
                <MenuItem Header="Back Art _Library" Command="{Binding ManageBackArtLibraryCommand}"/>
                <MenuItem Header="Clear _Cache" Command="{Binding ActiveProject.Inner.ClearCacheCommand}"/>
                <Separator/>
                <MenuItem Header="_Settings" Command="{Binding OpenSettingsCommand}"/>
            </MenuItem>
            <!-- Help -->
            <MenuItem Header="_Help">
                <MenuItem Header="Check for _Updates" Command="{Binding CheckForUpdateCommand}"/>
                <MenuItem Header="_About" Command="{Binding AboutCommand}"/>
            </MenuItem>
        </Menu>

        <!-- ==================== ICON TOOLBAR (Row 1) ==================== -->
        <Border Grid.Row="1" Background="#2D2D30" Padding="4,2">
            <DockPanel>
                <!-- Right-aligned buttons -->
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button Style="{StaticResource ToolbarIconBtn}" Content="&#xE8B9;"
                            AutomationProperties.Name="Art Library"
                            Command="{Binding ManageFrontArtLibraryCommand}" ToolTip="Art Library"/>
                    <Button Style="{StaticResource ToolbarIconBtn}" Content="&#xE713;"
                            AutomationProperties.Name="Settings"
                            Command="{Binding OpenSettingsCommand}" ToolTip="Settings"/>
                </StackPanel>
                <!-- Left-aligned button groups -->
                <StackPanel Orientation="Horizontal">
                    <!-- Group 1: New, Open, Save -->
                    <Button Style="{StaticResource ToolbarIconBtn}" Content="&#xE7C3;"
                            AutomationProperties.Name="New Project"
                            Command="{Binding NewProjectCommand}" ToolTip="New Project (Ctrl+N)"/>
                    <Button Style="{StaticResource ToolbarIconBtn}" Content="&#xE8E5;"
                            AutomationProperties.Name="Open Project"
                            Command="{Binding OpenProjectCommand}" ToolTip="Open Project (Ctrl+O)"/>
                    <Button Style="{StaticResource ToolbarIconBtn}" Content="&#xE74E;"
                            AutomationProperties.Name="Save"
                            Command="{Binding ActiveProject.Inner.SaveProjectCommand}" ToolTip="Save (Ctrl+S)"/>
                    <Border Width="1" Background="#444" Margin="4,4"/>
                    <!-- Group 2: Undo, Redo -->
                    <Button Style="{StaticResource ToolbarIconBtn}" Content="&#xE7A7;"
                            AutomationProperties.Name="Undo"
                            Command="{Binding ActiveProject.Inner.UndoCommand}" ToolTip="Undo (Ctrl+Z)"/>
                    <Button Style="{StaticResource ToolbarIconBtn}" Content="&#xE7A6;"
                            AutomationProperties.Name="Redo"
                            Command="{Binding ActiveProject.Inner.RedoCommand}" ToolTip="Redo (Ctrl+Y)"/>
                    <Border Width="1" Background="#444" Margin="4,4"/>
                    <!-- Group 3: Add File, Export PDF -->
                    <Button Style="{StaticResource ToolbarIconBtn}" Content="&#xE710;"
                            AutomationProperties.Name="Add Card from File"
                            Command="{Binding ActiveProject.Inner.AddCardFromFileCommand}" ToolTip="Add Card from File"/>
                    <Button Style="{StaticResource ToolbarIconBtn}" Content="&#xE8A5;"
                            AutomationProperties.Name="Export PDF"
                            Command="{Binding ActiveProject.Inner.ExportPdfCommand}" ToolTip="Export PDF (Ctrl+E)"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- ==================== PROJECT TAB BAR (Row 2) ==================== -->
        <Border Grid.Row="2" Background="#252526"
                Visibility="{Binding HasActiveProject, Converter={StaticResource BoolToVisibility}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Global file actions (always available) -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="4,2">
                    <Button Style="{StaticResource ToolBtn}" Command="{Binding NewProjectCommand}" Content="+ New" Padding="8,4"/>
                    <Button Style="{StaticResource ToolBtn}" Command="{Binding OpenProjectCommand}" Content="Open" Padding="8,4"/>
                    <Border Width="1" Background="#444" Margin="4,4"/>
                </StackPanel>

                <!-- Project tabs -->
                <ItemsControl Grid.Column="1" ItemsSource="{Binding Projects}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="10,5" Margin="1,2" CornerRadius="4,4,0,0" Cursor="Hand"
                                    MouseLeftButtonUp="OnTabClick" Tag="{Binding}"
                                    Background="#3E3E42">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding TabTitle}" Foreground="#DDD" FontSize="11" VerticalAlignment="Center"/>
                                    <Button Content="&#x2715;" Padding="4,0" Margin="6,0,0,0"
                                            Background="Transparent" Foreground="#888" BorderThickness="0"
                                            Cursor="Hand" FontSize="9" VerticalAlignment="Center"
                                            Click="OnTabClose" Tag="{Binding}"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Project name + Export (per-project) -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="4,2"
                            DataContext="{Binding ActiveProject.Inner}">
                    <TextBlock Text="Project:" Foreground="#999" VerticalAlignment="Center" Margin="0,0,6,0" FontSize="12"/>
                    <TextBox Text="{Binding ProjectName, UpdateSourceTrigger=PropertyChanged}"
                             Width="180" Padding="6,4" FontSize="12"
                             Background="#3E3E42" Foreground="White" BorderBrush="#555" BorderThickness="1"
                             CaretBrush="White"/>
                    <Button Style="{StaticResource AccentBtn}" Command="{Binding ExportPdfCommand}" Content="Export PDF" Margin="8,0,4,0" Padding="10,4"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ==================== UPDATE BANNER (Row 3) ==================== -->
        <Border Grid.Row="3" Background="#4A3800" Padding="10,6"
                Visibility="{Binding UpdateAvailable, Converter={StaticResource BoolToVisibility}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="&#x26A0;" Foreground="#FFB800" FontSize="14"
                           VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock Grid.Column="1" Text="{Binding UpdateMessage}" Foreground="#FFD" FontSize="12"
                           VerticalAlignment="Center"/>
                <Button Grid.Column="2" Content="Download" Padding="10,4" Margin="8,0"
                        Background="#0078D4" Foreground="White" BorderThickness="0" Cursor="Hand" FontSize="11"
                        Command="{Binding DownloadUpdateCommand}"/>
                <Button Grid.Column="3" Content="&#x2715;" Padding="6,2"
                        Background="Transparent" Foreground="#AA9" BorderThickness="0" Cursor="Hand" FontSize="12"
                        Command="{Binding DismissUpdateCommand}" ToolTip="Dismiss"/>
            </Grid>
        </Border>

        <!-- ==================== WELCOME SCREEN (Row 4, no project open) ==================== -->
        <Border Grid.Row="4" Background="#1E1E1E"
                Visibility="{Binding HasActiveProject, Converter={StaticResource InverseBoolToVisibility}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="400">
                <TextBlock Text="TCG Proxy Builder" Foreground="#555" FontSize="36" FontWeight="Light" HorizontalAlignment="Center"/>
                <TextBlock Text="Use File menu or Ctrl+N / Ctrl+O to get started" Foreground="#444" FontSize="12" HorizontalAlignment="Center" Margin="0,8,0,20"/>

                <!-- Recent files -->
                <Border Background="#252526" CornerRadius="6" Padding="16,12" Margin="0,0,0,16"
                        Visibility="{Binding HasRecentFiles, Converter={StaticResource BoolToVisibility}}">
                    <StackPanel>
                        <TextBlock Text="Recent Projects" Foreground="#888" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,8"/>
                        <ItemsControl ItemsSource="{Binding RecentFiles}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="6,4" Margin="0,1" CornerRadius="3" Cursor="Hand"
                                            Background="Transparent"
                                            MouseLeftButtonUp="OnRecentFileClick" Tag="{Binding}">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#3E3E42"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Converter={StaticResource FileNameConverter}}"
                                                       Foreground="#CCC" FontSize="12"/>
                                            <TextBlock Text="{Binding}" Foreground="#555" FontSize="9"
                                                       TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <TextBlock Foreground="#444" FontSize="11" HorizontalAlignment="Center">
                    <Run Text="v"/><Run Text="{Binding AppVersion, Mode=OneTime}"/>
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- ==================== MAIN PROJECT CONTENT (Row 4, project open) ==================== -->
        <Grid Grid.Row="4" DataContext="{Binding ActiveProject.Inner}"
              Visibility="{Binding DataContext.HasActiveProject, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVisibility}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition x:Name="SidebarColumn" Width="300" MinWidth="200" MaxWidth="600"/>
            </Grid.ColumnDefinitions>

            <!-- ===== CANVAS AREA (Column 0) ===== -->
            <Border Grid.Column="0" Background="#252526">
                <Grid>
                    <ScrollViewer x:Name="CanvasScrollViewer"
                                  HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                                  Background="#1E1E1E"
                                  PreviewMouseWheel="OnCanvasMouseWheel"
                                  PreviewMouseDown="OnCanvasMouseDown"
                                  PreviewMouseUp="OnCanvasMouseUp"
                                  PreviewMouseMove="OnCanvasMouseMove">
                        <Border Padding="30" HorizontalAlignment="Center">
                            <Border Effect="{StaticResource PageShadow}">
                                <controls:GridEditorCanvas x:Name="GridCanvas"
                                    PageSettings="{Binding CurrentProject.PageSettings}"
                                    CardsSource="{Binding FilteredCards}"
                                    ShowCutGuides="{Binding CurrentProject.PrintSettings.ShowCutGuides}"
                                    PrintSettingsSource="{Binding CurrentProject.PrintSettings}"
                                    SelectedCard="{Binding SelectedCard}"
                                    UndoSvc="{Binding UndoServiceInstance}"
                                    RefreshTrigger="{Binding RefreshTrigger}"
                                    Cursor="Hand">
                                    <controls:GridEditorCanvas.LayoutTransform>
                                        <ScaleTransform x:Name="CanvasScale" ScaleX="1" ScaleY="1"/>
                                    </controls:GridEditorCanvas.LayoutTransform>
                                </controls:GridEditorCanvas>
                            </Border>
                        </Border>
                    </ScrollViewer>

                    <!-- Render progress overlay -->
                    <Border Background="#CC1E1E1E" CornerRadius="6" Padding="16,10"
                            HorizontalAlignment="Center" VerticalAlignment="Center"
                            Visibility="{Binding IsRendering, ElementName=GridCanvas, Converter={StaticResource BoolToVisibility}}">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="{Binding RenderProgress, ElementName=GridCanvas}" FontSize="12"
                                       Foreground="#0078D4" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Border>

                    <!-- Zoom indicator -->
                    <Border Background="#CC2D2D30" CornerRadius="4" Padding="8,4"
                            HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,20,8">
                        <StackPanel Orientation="Horizontal">
                            <Button Content="-" Padding="6,1" FontSize="14" FontWeight="Bold"
                                    Background="Transparent" Foreground="#CCC" BorderThickness="0" Cursor="Hand"
                                    Click="ZoomOut"/>
                            <TextBlock x:Name="ZoomLabel" Text="100%" Foreground="#CCC" FontSize="11"
                                       VerticalAlignment="Center" Margin="8,0" MinWidth="36" TextAlignment="Center"/>
                            <Button Content="+" Padding="6,1" FontSize="14" FontWeight="Bold"
                                    Background="Transparent" Foreground="#CCC" BorderThickness="0" Cursor="Hand"
                                    Click="ZoomIn"/>
                            <Border Width="1" Background="#555" Margin="6,2"/>
                            <Button Content="Fit" Padding="6,1" FontSize="11"
                                    Background="Transparent" Foreground="#AAA" BorderThickness="0" Cursor="Hand"
                                    Click="ZoomFit"/>
                            <Button Content="1:1" Padding="6,1" FontSize="11"
                                    Background="Transparent" Foreground="#AAA" BorderThickness="0" Cursor="Hand"
                                    Click="ZoomReset"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- ===== GRID SPLITTER ===== -->
            <GridSplitter Grid.Column="1" Width="4" Background="#3E3E42" HorizontalAlignment="Center"
                          VerticalAlignment="Stretch" Cursor="SizeWE"/>

            <!-- ===== SIDEBAR (Column 2) ===== -->
            <Border Grid.Column="2" Background="#2D2D30" BorderBrush="#3E3E42" BorderThickness="1,0,0,0"
                    x:Name="SidebarBorder">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="SidebarContent">
                        <StackPanel.LayoutTransform>
                            <ScaleTransform
                                ScaleX="{Binding DataContext.SidebarScale, RelativeSource={RelativeSource AncestorType=Window}}"
                                ScaleY="{Binding DataContext.SidebarScale, RelativeSource={RelativeSource AncestorType=Window}}"/>
                        </StackPanel.LayoutTransform>

                        <!-- ===== SEARCH SECTION ===== -->
                        <controls:SidebarSection x:Name="SearchSection" Header="Search" IsExpanded="True">
                                <StackPanel Margin="10,8">
                                    <!-- Search box -->
                                    <Grid Margin="0,0,0,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox Text="{Binding ScryfallSearchQuery, UpdateSourceTrigger=PropertyChanged}"
                                                 Padding="6,5" FontSize="12"
                                                 Background="#3E3E42" Foreground="White" BorderBrush="#555" BorderThickness="1"
                                                 CaretBrush="White">
                                            <TextBox.InputBindings>
                                                <KeyBinding Key="Enter" Command="{Binding ScryfallSearchCommand}"/>
                                            </TextBox.InputBindings>
                                        </TextBox>
                                        <Button Grid.Column="1" Style="{StaticResource AccentBtn}"
                                                Command="{Binding ScryfallSearchCommand}" Content="Search" Margin="4,0,0,0" Padding="10,5"/>
                                    </Grid>

                                    <!-- Scryfall advanced search toggle -->
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        <CheckBox IsChecked="{Binding ShowAdvancedSearch}" Foreground="#AAA" FontSize="10" Content="Advanced Search"
                                                  VerticalContentAlignment="Center"/>
                                        <TextBlock Text="  Scryfall syntax" Foreground="#555" FontSize="9" VerticalAlignment="Center"
                                                   Visibility="{Binding ShowAdvancedSearch, Converter={StaticResource BoolToVisibility}}"/>
                                    </StackPanel>

                                    <!-- Advanced search panel (Scryfall only) -->
                                    <Border Background="#252526" CornerRadius="4" Padding="8" Margin="0,0,0,6">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ShowAdvancedSearch}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel>
                                            <TextBlock Text="Name" Foreground="#999" FontSize="9.5" Margin="0,0,0,1"/>
                                            <TextBox Text="{Binding AdvName, UpdateSourceTrigger=PropertyChanged}" Padding="4,3" FontSize="11"
                                                     Background="#3E3E42" Foreground="White" BorderBrush="#555" CaretBrush="White"/>

                                            <TextBlock Text="Type (e.g. creature, instant, legendary)" Foreground="#999" FontSize="9.5" Margin="0,4,0,1"/>
                                            <TextBox Text="{Binding AdvType, UpdateSourceTrigger=PropertyChanged}" Padding="4,3" FontSize="11"
                                                     Background="#3E3E42" Foreground="White" BorderBrush="#555" CaretBrush="White"/>

                                            <TextBlock Text="Rules text contains" Foreground="#999" FontSize="9.5" Margin="0,4,0,1"/>
                                            <TextBox Text="{Binding AdvOracle, UpdateSourceTrigger=PropertyChanged}" Padding="4,3" FontSize="11"
                                                     Background="#3E3E42" Foreground="White" BorderBrush="#555" CaretBrush="White"/>

                                            <Grid Margin="0,4,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="6"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="Colors (WUBRG)" Foreground="#999" FontSize="9.5" Margin="0,0,0,1"/>
                                                    <TextBox Text="{Binding AdvColors, UpdateSourceTrigger=PropertyChanged}" Padding="4,3" FontSize="11"
                                                             Background="#3E3E42" Foreground="White" BorderBrush="#555" CaretBrush="White"/>
                                                </StackPanel>
                                                <StackPanel Grid.Column="2">
                                                    <TextBlock Text="Identity (WUBRG)" Foreground="#999" FontSize="9.5" Margin="0,0,0,1"/>
                                                    <TextBox Text="{Binding AdvIdentity, UpdateSourceTrigger=PropertyChanged}" Padding="4,3" FontSize="11"
                                                             Background="#3E3E42" Foreground="White" BorderBrush="#555" CaretBrush="White"/>
                                                </StackPanel>
                                            </Grid>

                                            <Grid Margin="0,4,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="50"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="6"/>
                                                    <ColumnDefinition Width="50"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0" Grid.ColumnSpan="2">
                                                    <TextBlock Text="Mana Value" Foreground="#999" FontSize="9.5" Margin="0,0,0,1"/>
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="50"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <ComboBox ItemsSource="{Binding ComparisonOps}" SelectedItem="{Binding AdvCmcOp}" Padding="2" FontSize="10"/>
                                                        <TextBox Grid.Column="1" Text="{Binding AdvCmcValue, UpdateSourceTrigger=PropertyChanged}" Padding="4,3" FontSize="11"
                                                                 Background="#3E3E42" Foreground="White" BorderBrush="#555" CaretBrush="White" Margin="4,0,0,0"/>
                                                    </Grid>
                                                </StackPanel>
                                                <StackPanel Grid.Column="3" Grid.ColumnSpan="2">
                                                    <TextBlock Text="Rarity" Foreground="#999" FontSize="9.5" Margin="0,0,0,1"/>
                                                    <ComboBox ItemsSource="{Binding RarityAdvOptions}" SelectedItem="{Binding AdvRarity}" Padding="2" FontSize="10"/>
                                                </StackPanel>
                                            </Grid>

                                            <Grid Margin="0,4,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="6"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="Power" Foreground="#999" FontSize="9.5" Margin="0,0,0,1"/>
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="50"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <ComboBox ItemsSource="{Binding ComparisonOps}" SelectedItem="{Binding AdvPowOp}" Padding="2" FontSize="10"/>
                                                        <TextBox Grid.Column="1" Text="{Binding AdvPowValue, UpdateSourceTrigger=PropertyChanged}" Padding="4,3" FontSize="11"
                                                                 Background="#3E3E42" Foreground="White" BorderBrush="#555" CaretBrush="White" Margin="4,0,0,0"/>
                                                    </Grid>
                                                </StackPanel>
                                                <StackPanel Grid.Column="2">
                                                    <TextBlock Text="Toughness" Foreground="#999" FontSize="9.5" Margin="0,0,0,1"/>
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="50"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <ComboBox ItemsSource="{Binding ComparisonOps}" SelectedItem="{Binding AdvTouOp}" Padding="2" FontSize="10"/>
                                                        <TextBox Grid.Column="1" Text="{Binding AdvTouValue, UpdateSourceTrigger=PropertyChanged}" Padding="4,3" FontSize="11"
                                                                 Background="#3E3E42" Foreground="White" BorderBrush="#555" CaretBrush="White" Margin="4,0,0,0"/>
                                                    </Grid>
                                                </StackPanel>
                                            </Grid>

                                            <Grid Margin="0,4,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="6"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="Set code" Foreground="#999" FontSize="9.5" Margin="0,0,0,1"/>
                                                    <TextBox Text="{Binding AdvSet, UpdateSourceTrigger=PropertyChanged}" Padding="4,3" FontSize="11"
                                                             Background="#3E3E42" Foreground="White" BorderBrush="#555" CaretBrush="White"/>
                                                </StackPanel>
                                                <StackPanel Grid.Column="2">
                                                    <TextBlock Text="Format" Foreground="#999" FontSize="9.5" Margin="0,0,0,1"/>
                                                    <ComboBox ItemsSource="{Binding FormatOptions}" SelectedItem="{Binding AdvFormat}" Padding="2" FontSize="10"/>
                                                </StackPanel>
                                            </Grid>

                                            <Grid Margin="0,4,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="6"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="Artist" Foreground="#999" FontSize="9.5" Margin="0,0,0,1"/>
                                                    <TextBox Text="{Binding AdvArtist, UpdateSourceTrigger=PropertyChanged}" Padding="4,3" FontSize="11"
                                                             Background="#3E3E42" Foreground="White" BorderBrush="#555" CaretBrush="White"/>
                                                </StackPanel>
                                                <StackPanel Grid.Column="2">
                                                    <TextBlock Text="Keyword" Foreground="#999" FontSize="9.5" Margin="0,0,0,1"/>
                                                    <TextBox Text="{Binding AdvKeyword, UpdateSourceTrigger=PropertyChanged}" Padding="4,3" FontSize="11"
                                                             Background="#3E3E42" Foreground="White" BorderBrush="#555" CaretBrush="White"/>
                                                </StackPanel>
                                            </Grid>

                                            <TextBlock Text="Card is..." Foreground="#999" FontSize="9.5" Margin="0,4,0,1"/>
                                            <ComboBox ItemsSource="{Binding IsOptions}" SelectedItem="{Binding AdvIs}" Padding="2" FontSize="10" Margin="0,0,0,6"/>

                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="4"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Button Grid.Column="0" Style="{StaticResource AccentBtn}"
                                                        Command="{Binding BuildAdvancedQueryCommand}" Content="Build &amp; Search" Padding="6,5"/>
                                                <Button Grid.Column="2" Style="{StaticResource ToolBtn}"
                                                        Command="{Binding ClearAdvancedSearchCommand}" Content="Clear" Padding="6,5"/>
                                            </Grid>

                                            <TextBlock Text="Query:" Foreground="#666" FontSize="9" Margin="0,6,0,1"/>
                                            <TextBlock Text="{Binding ScryfallSearchQuery}" Foreground="#0078D4" FontSize="10"
                                                       TextWrapping="Wrap" MaxHeight="40"/>
                                        </StackPanel>
                                    </Border>

                                    <!-- Scryfall results (with thumbnails) -->
                                    <ListBox ItemsSource="{Binding ScryfallResults}" SelectedItem="{Binding SelectedScryfallCard}"
                                             Height="180" BorderThickness="1" BorderBrush="#444"
                                             Background="#252526" FontSize="11"
                                             MouseDoubleClick="OnScryfallDoubleClick">
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" Margin="2">
                                                    <Border Width="36" Height="50" Background="#111" CornerRadius="3" Margin="0,0,6,0">
                                                        <Image Source="{Binding ImageUris[small]}" Stretch="UniformToFill"/>
                                                    </Border>
                                                    <StackPanel VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding Name}" FontSize="10.5" Foreground="#DDD" TextTrimming="CharacterEllipsis" MaxWidth="180"/>
                                                        <TextBlock FontSize="9" Foreground="#888">
                                                            <Run Text="{Binding SetName, Mode=OneWay}"/>
                                                            <Run Text=" #"/><Run Text="{Binding CollectorNumber, Mode=OneWay}"/>
                                                        </TextBlock>
                                                    </StackPanel>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>

                                    <!-- Preview panel (shown when a result is selected) -->
                                    <Border Height="200" Background="#111" CornerRadius="4" Margin="0,6,0,0"
                                            Visibility="{Binding SearchPreviewUrl, Converter={StaticResource NullToCollapsed}, FallbackValue=Collapsed}">
                                        <Image Source="{Binding SearchPreviewUrl}" Stretch="Uniform" Margin="4"/>
                                    </Border>

                                    <!-- Add button -->
                                    <Button Style="{StaticResource AccentBtn}" Command="{Binding AddScryfallCardCommand}"
                                            Content="Add Selected Card" Margin="0,6,0,0" HorizontalAlignment="Stretch"/>

                                    <Button Style="{StaticResource ToolBtn}" Command="{Binding UpdateAllArtFromMpcFillCommand}"
                                            Content="Update All Art from MPCFill" Margin="0,6,0,0" HorizontalAlignment="Stretch"/>
                                </StackPanel>
                        </controls:SidebarSection>

                        <!-- ===== IMPORT SECTION ===== -->
                        <controls:SidebarSection x:Name="ImportSection" Header="Import">
                                <StackPanel Margin="10,8">
                                    <!-- Moxfield import -->
                                    <TextBlock Text="Import Deck" FontWeight="SemiBold" Foreground="#CCC" Margin="0,0,0,6" FontSize="12"/>
                                    <TextBlock Text="Paste a URL from Moxfield, Archidekt, or Piltover Archive" Foreground="#666" FontSize="10" Margin="0,0,0,4"/>
                                    <Grid Margin="0,0,0,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox Text="{Binding ImportDeckUrl, UpdateSourceTrigger=PropertyChanged}"
                                                 Padding="6,5" FontSize="11"
                                                 Background="#3E3E42" Foreground="White" BorderBrush="#555" BorderThickness="1"
                                                 CaretBrush="White">
                                            <TextBox.InputBindings>
                                                <KeyBinding Key="Enter" Command="{Binding ImportDeckCommand}"/>
                                            </TextBox.InputBindings>
                                        </TextBox>
                                        <Button Grid.Column="1" Style="{StaticResource AccentBtn}"
                                                Command="{Binding ImportDeckCommand}" Content="Import" Margin="4,0,0,0" Padding="10,5"/>
                                    </Grid>
                                    <CheckBox IsChecked="{Binding IgnoreDuplicates}" Content="Skip cards already in project (merge basics)"
                                              Foreground="#999" FontSize="10" Margin="0,2,0,0"
                                              ToolTip="Cards already in the project are skipped. Basic lands are only added if the deck needs more copies than you already have."/>

                                    <!-- Refresh deck (visible when a deck URL is stored) -->
                                    <Border Margin="0,8,0,0" Background="#252526" CornerRadius="4" Padding="8,6"
                                            Visibility="{Binding HasDeckImportUrl, Converter={StaticResource BoolToVisibility}}">
                                        <StackPanel>
                                            <TextBlock Text="Imported Deck" Foreground="#999" FontSize="9" Margin="0,0,0,2"/>
                                            <TextBlock Text="{Binding DeckImportUrlDisplay}" Foreground="#888" FontSize="10"
                                                       TextTrimming="CharacterEllipsis" ToolTip="{Binding DeckImportUrlDisplay}" Margin="0,0,0,4"/>
                                            <Button Style="{StaticResource AccentBtn}" Command="{Binding RefreshDeckCommand}"
                                                    Content="Refresh Deck" HorizontalAlignment="Stretch" Padding="8,4"/>
                                        </StackPanel>
                                    </Border>

                                    <TextBlock Text="Import Text List" FontWeight="SemiBold" Foreground="#CCC" Margin="0,16,0,6" FontSize="12"/>
                                    <Button Style="{StaticResource AccentBtn}" Command="{Binding ImportTextListCommand}"
                                            Content="Import Card List..." HorizontalAlignment="Stretch" Padding="8,6"/>
                                    <TextBlock Text="Paste a list of cards (1 per line)" Foreground="#666" FontSize="10" Margin="0,4,0,0"/>

                                    <TextBlock Text="Import MPCFill Project" FontWeight="SemiBold" Foreground="#CCC" Margin="0,16,0,6" FontSize="12"/>
                                    <Button Style="{StaticResource AccentBtn}" Command="{Binding ImportMpcFillXmlCommand}"
                                            Content="Import cards.xml..." HorizontalAlignment="Stretch" Padding="8,6"/>
                                    <TextBlock Text="Import a cards.xml file exported from MPCFill" Foreground="#666" FontSize="10" Margin="0,4,0,0"/>
                                </StackPanel>
                        </controls:SidebarSection>

                        <!-- ===== CARD DETAILS SECTION ===== -->
                        <controls:SidebarSection x:Name="CardDetailsSection" Header="Card Details">
                                <StackPanel Margin="10,8" DataContext="{Binding SelectedCard}">
                                    <TextBlock Text="(Click a card on the page)" Foreground="#666" FontStyle="Italic" FontSize="12" Margin="0,0,0,8">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding}" Value="{x:Null}">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>

                                    <StackPanel>
                                        <StackPanel.Style>
                                            <Style TargetType="StackPanel">
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding}" Value="{x:Null}">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>

                                        <!-- Face indicator (visible for double-faced cards) -->
                                        <Border Margin="0,0,0,6" Padding="6,3" Background="#252526" CornerRadius="3"
                                                Visibility="{Binding IsDoubleFaced, Converter={StaticResource BoolToVisibility}}">
                                            <TextBlock FontSize="9" Foreground="#888">
                                                <Run Text="Showing:"/>
                                                <Run Text="{Binding DataContext.ActiveProject.Inner.IsShowingBackFace, RelativeSource={RelativeSource AncestorType=Window}, Mode=OneWay, Converter={StaticResource BoolToFaceText}}"
                                                     Foreground="#0078D4" FontWeight="SemiBold"/>
                                                <Run Text=" — flip card on canvas to switch"/>
                                            </TextBlock>
                                        </Border>

                                        <!-- Name -->
                                        <TextBlock Text="Name" Foreground="#999" FontSize="10" Margin="0,0,0,2"/>
                                        <TextBlock Text="{Binding DataContext.ActiveProject.Inner.DisplayName, RelativeSource={RelativeSource AncestorType=Window}}"
                                                   Foreground="White" FontSize="12" Padding="6,4" Margin="0,0,0,8"/>

                                        <!-- Scryfall Lookup (visible when ScryfallId is empty, hidden for pasted/Riftbound) -->
                                        <Border Margin="0,0,0,8" Background="#252526" CornerRadius="4" Padding="8,6">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ScryfallId}" Value="{x:Null}">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding ScryfallId}" Value="">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsPastedImage}" Value="True">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsRiftbound}" Value="True">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <StackPanel>
                                                <TextBlock Text="Fetch Card Data from Scryfall" Foreground="#CCC" FontSize="10" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBox Grid.Column="0" Padding="6,4" FontSize="11"
                                                             Text="{Binding DataContext.ActiveProject.Inner.ScryfallLookupText, RelativeSource={RelativeSource AncestorType=Window}, UpdateSourceTrigger=PropertyChanged}"
                                                             Background="#3E3E42" Foreground="White" BorderBrush="#555" CaretBrush="White" Margin="0,0,6,0"/>
                                                    <Button Grid.Column="1" Content="Fetch" Padding="10,4"
                                                            Style="{StaticResource AccentBtn}"
                                                            Command="{Binding DataContext.ActiveProject.Inner.FetchScryfallDataCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
                                                </Grid>
                                                <TextBlock Text="{Binding DataContext.ActiveProject.Inner.ScryfallLookupStatus, RelativeSource={RelativeSource AncestorType=Window}}"
                                                           Foreground="#888" FontSize="9" Margin="0,4,0,0" TextWrapping="Wrap"/>
                                            </StackPanel>
                                        </Border>

                                        <!-- Quantity / Mana Cost -->
                                        <Grid Margin="0,0,0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0" Margin="0,0,4,0">
                                                <TextBlock Text="Quantity" Foreground="#999" FontSize="10" Margin="0,0,0,2"/>
                                                <TextBox Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" Padding="6,4"
                                                         Background="#3E3E42" Foreground="White" BorderBrush="#555" CaretBrush="White"/>
                                            </StackPanel>
                                            <StackPanel Grid.Column="1" Margin="4,0,0,0">
                                                <TextBlock Text="Mana Cost" Foreground="#999" FontSize="10" Margin="0,0,0,2"/>
                                                <controls:ManaTextBlock ManaText="{Binding DataContext.ActiveProject.Inner.DisplayManaCost, RelativeSource={RelativeSource AncestorType=Window}}" Foreground="#CCC" FontSize="12" SymbolSize="14" Padding="6,5"/>
                                            </StackPanel>
                                        </Grid>

                                        <!-- CMC -->
                                        <TextBlock Text="Mana Value" Foreground="#999" FontSize="10" Margin="0,0,0,2"/>
                                        <TextBlock Text="{Binding CMC}" Foreground="#CCC" FontSize="11" Margin="0,0,0,8"/>

                                        <!-- Type Line -->
                                        <TextBlock Text="Type" Foreground="#999" FontSize="10" Margin="0,0,0,2"/>
                                        <TextBlock Text="{Binding DataContext.ActiveProject.Inner.DisplayTypeLine, RelativeSource={RelativeSource AncestorType=Window}}" Foreground="#AAA" FontSize="11" Margin="0,0,0,8" TextWrapping="Wrap"/>

                                        <!-- Oracle Text -->
                                        <TextBlock Text="Oracle Text" Foreground="#999" FontSize="10" Margin="0,0,0,2"/>
                                        <controls:ManaTextBlock ManaText="{Binding DataContext.ActiveProject.Inner.DisplayOracleText, RelativeSource={RelativeSource AncestorType=Window}}" Foreground="#888" FontSize="10" SymbolSize="12" TextWrapping="Wrap" Margin="0,0,0,8"/>

                                        <!-- Power / Toughness (creatures only) -->
                                        <Grid Margin="0,0,0,8">
                                            <Grid.Style>
                                                <Style TargetType="Grid">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding DataContext.ActiveProject.Inner.DisplayPower, RelativeSource={RelativeSource AncestorType=Window}}" Value="">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Grid.Style>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0" Margin="0,0,4,0">
                                                <TextBlock Text="Power" Foreground="#999" FontSize="10" Margin="0,0,0,2"/>
                                                <TextBlock Text="{Binding DataContext.ActiveProject.Inner.DisplayPower, RelativeSource={RelativeSource AncestorType=Window}}" Foreground="#CCC" FontSize="11"/>
                                            </StackPanel>
                                            <StackPanel Grid.Column="1" Margin="4,0,0,0">
                                                <TextBlock Text="Toughness" Foreground="#999" FontSize="10" Margin="0,0,0,2"/>
                                                <TextBlock Text="{Binding DataContext.ActiveProject.Inner.DisplayToughness, RelativeSource={RelativeSource AncestorType=Window}}" Foreground="#CCC" FontSize="11"/>
                                            </StackPanel>
                                        </Grid>

                                        <!-- Loyalty (planeswalkers only) -->
                                        <StackPanel Margin="0,0,0,8">
                                            <StackPanel.Style>
                                                <Style TargetType="StackPanel">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding DataContext.ActiveProject.Inner.DisplayLoyalty, RelativeSource={RelativeSource AncestorType=Window}}" Value="">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </StackPanel.Style>
                                            <TextBlock Text="Loyalty" Foreground="#999" FontSize="10" Margin="0,0,0,2"/>
                                            <TextBlock Text="{Binding DataContext.ActiveProject.Inner.DisplayLoyalty, RelativeSource={RelativeSource AncestorType=Window}}" Foreground="#CCC" FontSize="11"/>
                                        </StackPanel>

                                        <!-- Colors / Color Identity -->
                                        <Grid Margin="0,0,0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0" Margin="0,0,4,0">
                                                <TextBlock Text="Colors" Foreground="#999" FontSize="10" Margin="0,0,0,2"/>
                                                <TextBlock Text="{Binding Colors}" Foreground="#CCC" FontSize="11"/>
                                            </StackPanel>
                                            <StackPanel Grid.Column="1" Margin="4,0,0,0">
                                                <TextBlock Text="Color Identity" Foreground="#999" FontSize="10" Margin="0,0,0,2"/>
                                                <TextBlock Text="{Binding ColorIdentity}" Foreground="#CCC" FontSize="11"/>
                                            </StackPanel>
                                        </Grid>

                                        <!-- Keywords (only when present) -->
                                        <StackPanel Margin="0,0,0,8">
                                            <StackPanel.Style>
                                                <Style TargetType="StackPanel">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Keywords}" Value="">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </StackPanel.Style>
                                            <TextBlock Text="Keywords" Foreground="#999" FontSize="10" Margin="0,0,0,2"/>
                                            <TextBlock Text="{Binding Keywords}" Foreground="#CCC" FontSize="10" TextWrapping="Wrap"/>
                                        </StackPanel>

                                        <!-- Set / Collector # with set symbol -->
                                        <Grid Margin="0,0,0,4">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <!-- Set symbol icon -->
                                            <controls:SetSymbolImage Grid.Column="0"
                                                SetCode="{Binding SetCode}" Rarity="{Binding Rarity}"
                                                SymbolSize="20" Margin="0,0,6,0" VerticalAlignment="Center"
                                                ToolTip="{Binding Rarity}"/>
                                            <StackPanel Grid.Column="1" Margin="0,0,4,0">
                                                <TextBlock Text="Set" Foreground="#999" FontSize="10" Margin="0,0,0,2"/>
                                                <TextBlock Foreground="#CCC" FontSize="10" TextWrapping="Wrap">
                                                    <Run Text="{Binding SetName, Mode=OneWay}"/>
                                                    <Run Text="{Binding SetCode, Mode=OneWay, StringFormat='({0})'}" Foreground="#888"/>
                                                </TextBlock>
                                            </StackPanel>
                                            <StackPanel Grid.Column="2" Margin="4,0,0,0">
                                                <TextBlock Text="#" Foreground="#999" FontSize="10" Margin="0,0,0,2"/>
                                                <TextBlock Text="{Binding CollectorNumber}" Foreground="#CCC" FontSize="10"/>
                                            </StackPanel>
                                        </Grid>

                                        <!-- Rarity / Artist -->
                                        <Grid Margin="0,0,0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0" Margin="0,0,4,0">
                                                <TextBlock Text="Rarity" Foreground="#999" FontSize="10" Margin="0,0,0,2"/>
                                                <TextBlock Text="{Binding Rarity}" Foreground="#CCC" FontSize="10"/>
                                            </StackPanel>
                                            <StackPanel Grid.Column="1" Margin="4,0,0,0">
                                                <TextBlock Text="Artist" Foreground="#999" FontSize="10" Margin="0,0,0,2"/>
                                                <TextBlock Text="{Binding Artist}" Foreground="#CCC" FontSize="10" TextTrimming="CharacterEllipsis"/>
                                            </StackPanel>
                                        </Grid>

                                        <!-- Select Art / Refresh Data (hidden for pasted/Riftbound) -->
                                        <Grid Margin="0,0,0,6">
                                            <Grid.Style>
                                                <Style TargetType="Grid">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsPastedImage}" Value="True">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsRiftbound}" Value="True">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Grid.Style>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="4"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <Button Grid.Column="0" Style="{StaticResource AccentBtn}"
                                                    Command="{Binding DataContext.ActiveProject.Inner.BrowseFrontArtworkCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    Content="Select Art..." HorizontalAlignment="Stretch" Padding="8,6"/>
                                            <Button Grid.Column="2" Style="{StaticResource ToolBtn}"
                                                    Command="{Binding DataContext.ActiveProject.Inner.RefreshSelectedCardDataCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    Content="&#xE72C;" FontFamily="Segoe MDL2 Assets" FontSize="13"
                                                    ToolTip="Refresh card data from Scryfall" Padding="6,6" Width="32" Height="32"/>
                                        </Grid>

                                        <TextBlock Text="Front Artwork" Foreground="#999" FontSize="10" Margin="0,0,0,2"/>
                                        <Border Height="150" Background="#111" CornerRadius="4" Margin="0,0,0,4">
                                            <Image Source="{Binding ArtworkPath, Converter={StaticResource FilePathToImage}}" Stretch="Uniform" Margin="4"/>
                                        </Border>
                                        <TextBlock Text="{Binding ArtworkPath}" Foreground="#555" FontSize="9"
                                                   TextTrimming="CharacterEllipsis" Margin="0,0,0,10"/>

                                        <!-- Overlay Text -->
                                        <TextBlock Text="Overlay Text" Foreground="#999" FontSize="10" Margin="0,4,0,2"/>
                                        <TextBox Text="{Binding OverlayText, UpdateSourceTrigger=PropertyChanged}" Padding="6,4" FontSize="12"
                                                 Background="#3E3E42" Foreground="White" BorderBrush="#555" CaretBrush="White" Margin="0,0,0,8"/>

                                        <CheckBox Content="Double-Faced Card" IsChecked="{Binding IsDoubleFaced}" Foreground="#CCC" Margin="0,0,0,4"/>
                                        <CheckBox Content="Include Back Artwork" IsChecked="{Binding IncludeBack}" Foreground="#CCC" Margin="0,0,0,6"/>
                                        <TextBlock Text="Back Artwork" Foreground="#999" FontSize="10" Margin="0,0,0,2"/>
                                        <Border Height="150" Background="#111" CornerRadius="4" Margin="0,0,0,4">
                                            <Image Source="{Binding BackArtworkPath, Converter={StaticResource FilePathToImage}}" Stretch="Uniform" Margin="4"/>
                                        </Border>
                                        <TextBlock Text="{Binding BackArtworkPath}" Foreground="#555" FontSize="9"
                                                   TextTrimming="CharacterEllipsis" Margin="0,0,0,8"/>
                                    </StackPanel>

                                    <!-- Back Art -->
                                    <TextBlock Text="BACK ART" Foreground="#0078D4" FontSize="10" FontWeight="Bold" Margin="0,8,0,6"/>
                                    <Button Style="{StaticResource AccentBtn}"
                                            Command="{Binding DataContext.ActiveProject.Inner.SelectBackArtForAllCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            Content="Select Back for All Cards..." HorizontalAlignment="Stretch" Padding="8,6" Margin="0,0,0,4"/>
                                    <Button Style="{StaticResource ToolBtn}"
                                            Command="{Binding DataContext.ActiveProject.Inner.SelectBackArtForMissingCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            Content="Select Back for Cards Without Back..." HorizontalAlignment="Stretch" Padding="8,6"
                                            Background="#3E3E42" Margin="0,0,0,4"/>
                                    <Button Style="{StaticResource DangerButton}"
                                            Command="{Binding DataContext.ActiveProject.Inner.ClearBackArtFromNonDfcCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            Content="Clear Back Art (non-DFC only)" HorizontalAlignment="Stretch" Padding="8,6"/>

                                    </StackPanel>
                        </controls:SidebarSection>

                        <!-- ===== FILTER SECTION ===== -->
                        <controls:SidebarSection x:Name="FilterSection" Header="Filter">
                                <StackPanel Margin="10,8">
                                    <TextBlock Text="Type filter expressions (e.g. name:Bolt, tag:rare, tag:dfc)" Foreground="#666" FontSize="9" Margin="0,0,0,4"/>
                                    <controls:PillFilterBar x:Name="CardFilterBar" Margin="0,0,0,6"/>
                                    <TextBlock x:Name="FilterCountLabel" Foreground="#888" FontSize="11">
                                        <Run Text="Showing "/>
                                        <Run Text="{Binding DataContext.ActiveProject.Inner.FilteredCards.Count, RelativeSource={RelativeSource AncestorType=Window}, Mode=OneWay}"/>
                                        <Run Text=" of "/>
                                        <Run Text="{Binding DataContext.ActiveProject.Inner.Cards.Count, RelativeSource={RelativeSource AncestorType=Window}, Mode=OneWay}"/>
                                        <Run Text=" cards"/>
                                    </TextBlock>
                                </StackPanel>
                        </controls:SidebarSection>

                        <!-- ===== SORT SECTION ===== -->
                        <controls:SidebarSection x:Name="SortSection" Header="Sort">
                                <StackPanel Margin="10,8">
                                    <TextBlock Text="Add sort fields in priority order (e.g. CMC, Name:desc)" Foreground="#666" FontSize="9" Margin="0,0,0,4"/>
                                    <controls:SortPillBar x:Name="CardSortBar" Margin="0,0,0,6"/>
                                    <UniformGrid Columns="2" Margin="0,0,0,4">
                                        <Button Style="{StaticResource AccentBtn}" Command="{Binding DataContext.ActiveProject.Inner.ApplySortToProjectCommand, RelativeSource={RelativeSource AncestorType=Window}}" Content="Apply to Project" Margin="0,0,2,0"/>
                                        <Button Style="{StaticResource ToolBtn}" Content="Clear All" Margin="2,0,0,0" Click="OnClearFilterSort"/>
                                    </UniformGrid>
                                </StackPanel>
                        </controls:SidebarSection>

                        <!-- ===== LAYOUT SECTION ===== -->
                        <controls:SidebarSection x:Name="LayoutSection" Header="Layout">
                                <StackPanel Margin="10,8">
                                    <!-- Page -->
                                    <TextBlock Text="PAGE" Foreground="#0078D4" FontSize="10" FontWeight="Bold" Margin="0,0,0,6"/>
                                    <TextBlock Text="Page Size" Foreground="#999" FontSize="10" Margin="0,0,0,2"/>
                                    <ComboBox ItemsSource="{Binding PagePresets}" SelectedItem="{Binding SelectedPagePreset}"
                                              Padding="4" Margin="0,0,0,6" FontSize="11"/>
                                    <!-- Custom dimensions (visible when "Custom" selected) -->
                                    <Grid Margin="0,0,0,6" Visibility="{Binding IsCustomPageSize, Converter={StaticResource BoolToVisibility}}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="8"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="Width" Foreground="#999" FontSize="9" Margin="0,0,0,2"/>
                                            <TextBox Text="{Binding CustomPageWidth, UpdateSourceTrigger=LostFocus}"
                                                     Padding="4,3" FontSize="10"
                                                     Background="#3E3E42" Foreground="White" BorderBrush="#555" CaretBrush="White"
                                                     ToolTip="e.g. 210 mm, 8.5&quot;, 8.5 in"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="Height" Foreground="#999" FontSize="9" Margin="0,0,0,2"/>
                                            <TextBox Text="{Binding CustomPageHeight, UpdateSourceTrigger=LostFocus}"
                                                     Padding="4,3" FontSize="10"
                                                     Background="#3E3E42" Foreground="White" BorderBrush="#555" CaretBrush="White"
                                                     ToolTip="e.g. 297 mm, 11&quot;, 11 in"/>
                                        </StackPanel>
                                    </Grid>
                                    <CheckBox Content="Landscape" IsChecked="{Binding CurrentProject.PageSettings.IsLandscape}" Foreground="#CCC" Margin="0,0,0,10"/>

                                    <!-- Print -->
                                    <TextBlock Text="PRINT" Foreground="#0078D4" FontSize="10" FontWeight="Bold" Margin="0,6,0,6"/>
                                    <TextBlock Text="Print Mode" Foreground="#999" FontSize="10" Margin="0,0,0,2"/>
                                    <ComboBox ItemsSource="{Binding PrintModeValues}" SelectedItem="{Binding SelectedPrintMode}"
                                              Padding="4" Margin="0,0,0,6" FontSize="11"/>
                                    <CheckBox Content="Cutting guides (edge-to-edge)" IsChecked="{Binding CurrentProject.PrintSettings.ShowCutGuides}" Foreground="#CCC" Margin="0,0,0,6"/>

                                    <!-- Printer Profile -->
                                    <TextBlock Text="Printer Profile" Foreground="#999" FontSize="10" Margin="0,6,0,2"/>
                                    <ComboBox ItemsSource="{Binding PrinterProfileNames}"
                                              SelectedItem="{Binding SelectedPrinterProfileName}"
                                              Padding="4" Margin="0,0,0,2" FontSize="11"/>
                                    <TextBlock Text="{Binding SelectedPrinterOffsetDisplay}" Foreground="#666" FontSize="9" Margin="0,0,0,6"/>

                                    <!-- Card Outline Guides -->
                                    <TextBlock Text="CARD OUTLINE" Foreground="#0078D4" FontSize="10" FontWeight="Bold" Margin="0,6,0,6"/>
                                    <CheckBox Content="Show card outline guides" IsChecked="{Binding CurrentProject.PrintSettings.ShowCardOutline}" Foreground="#CCC" Margin="0,0,0,6"/>

                                    <Grid Margin="0,0,0,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="6"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="Color" Foreground="#999" FontSize="9"/>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="0" Width="24" Height="24" CornerRadius="3" Margin="0,0,4,0"
                                                        BorderBrush="#555" BorderThickness="1" Cursor="Hand"
                                                        Background="{Binding CurrentProject.PrintSettings.OutlineColor}"
                                                        MouseLeftButtonUp="OnOutlineColorClick"/>
                                                <TextBox Grid.Column="1" Text="{Binding CurrentProject.PrintSettings.OutlineColor, UpdateSourceTrigger=LostFocus}" Padding="4,3" FontSize="11"
                                                         Background="#3E3E42" Foreground="White" BorderBrush="#555" CaretBrush="White"/>
                                            </Grid>
                                        </StackPanel>
                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="Line Weight (pt)" Foreground="#999" FontSize="9"/>
                                            <TextBox Text="{Binding CurrentProject.PrintSettings.LineWeight, UpdateSourceTrigger=LostFocus}" Padding="4,3" FontSize="11"
                                                     Background="#3E3E42" Foreground="White" BorderBrush="#555" CaretBrush="White"/>
                                        </StackPanel>
                                    </Grid>

                                    <Grid Margin="0,4,0,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="6"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="Alignment" Foreground="#999" FontSize="9"/>
                                            <ComboBox ItemsSource="{Binding OutlineAlignmentOptions}" SelectedItem="{Binding SelectedOutlineAlignment}" Padding="3" FontSize="10"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="Outline Type" Foreground="#999" FontSize="9"/>
                                            <ComboBox ItemsSource="{Binding OutlineTypeOptions}" SelectedItem="{Binding SelectedOutlineType}" Padding="3" FontSize="10"/>
                                        </StackPanel>
                                    </Grid>

                                    <Grid Margin="0,0,0,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="6"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="6"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="Corner Radius (mm)" Foreground="#999" FontSize="9"/>
                                            <TextBox Text="{Binding CurrentProject.PrintSettings.CornerRadiusMm, UpdateSourceTrigger=LostFocus}" Padding="4,3" FontSize="11"
                                                     Background="#3E3E42" Foreground="White" BorderBrush="#555" CaretBrush="White"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="Line Type" Foreground="#999" FontSize="9"/>
                                            <ComboBox ItemsSource="{Binding LineTypeOptions}" SelectedItem="{Binding SelectedLineType}" Padding="3" FontSize="10"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="4">
                                            <TextBlock Text="Corner Length (mm)" Foreground="#999" FontSize="9"/>
                                            <TextBox Text="{Binding CurrentProject.PrintSettings.CornerLengthMm, UpdateSourceTrigger=LostFocus}" Padding="4,3" FontSize="11"
                                                     Background="#3E3E42" Foreground="White" BorderBrush="#555" CaretBrush="White"/>
                                        </StackPanel>
                                    </Grid>

                                    <Separator Margin="0,4"/>

                                    <!-- Silhouette Cameo -->
                                    <TextBlock Text="SILHOUETTE CAMEO" Foreground="#0078D4" FontSize="10" FontWeight="Bold" Margin="0,6,0,6"/>
                                    <CheckBox Content="Add registration marks to PDF" IsChecked="{Binding CurrentProject.PrintSettings.ShowRegistrationMarks}"
                                              Foreground="#CCC" Margin="0,0,0,4"/>
                                    <CheckBox Content="Export SVG cut lines with PDF" IsChecked="{Binding CurrentProject.PrintSettings.ExportSvgCutLines}"
                                              Foreground="#CCC" Margin="0,0,0,4"/>
                                    <Button Content="Export SVG Only" Command="{Binding ExportSvgCommand}"
                                            Background="#3E3E42" Foreground="#CCC" BorderThickness="0"
                                            Padding="8,4" FontSize="11" Cursor="Hand" HorizontalAlignment="Left"
                                            Margin="0,0,0,6"/>
                                    <Grid Margin="0,0,0,4"
                                          Visibility="{Binding CurrentProject.PrintSettings.ShowRegistrationMarks, Converter={StaticResource BoolToVisibility}}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="8"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="8"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="8"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="Square (in)" Foreground="#999" FontSize="9"/>
                                            <TextBox Text="{Binding CurrentProject.PrintSettings.RegMarkSquareSizeIn, UpdateSourceTrigger=LostFocus}" Padding="4,3" FontSize="11"
                                                     Background="#3E3E42" Foreground="White" BorderBrush="#555" CaretBrush="White"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="Arm Length (in)" Foreground="#999" FontSize="9"/>
                                            <TextBox Text="{Binding CurrentProject.PrintSettings.RegMarkLengthIn, UpdateSourceTrigger=LostFocus}" Padding="4,3" FontSize="11"
                                                     Background="#3E3E42" Foreground="White" BorderBrush="#555" CaretBrush="White"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="4">
                                            <TextBlock Text="Thickness (in)" Foreground="#999" FontSize="9"/>
                                            <TextBox Text="{Binding CurrentProject.PrintSettings.RegMarkThicknessIn, UpdateSourceTrigger=LostFocus}" Padding="4,3" FontSize="11"
                                                     Background="#3E3E42" Foreground="White" BorderBrush="#555" CaretBrush="White"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="6">
                                            <TextBlock Text="Inset (in)" Foreground="#999" FontSize="9"/>
                                            <TextBox Text="{Binding CurrentProject.PrintSettings.RegMarkInsetIn, UpdateSourceTrigger=LostFocus}" Padding="4,3" FontSize="11"
                                                     Background="#3E3E42" Foreground="White" BorderBrush="#555" CaretBrush="White"/>
                                        </StackPanel>
                                    </Grid>

                                    <Separator Margin="0,4"/>

                                    <!-- Card Size -->
                                    <TextBlock Text="CARD SIZE" Foreground="#0078D4" FontSize="10" FontWeight="Bold" Margin="0,6,0,6"/>
                                    <ComboBox ItemsSource="{Binding CardSizePresets}" SelectedItem="{Binding SelectedCardSize}"
                                              Padding="4" Margin="0,0,0,6" FontSize="11"/>
                                    <Grid Margin="0,0,0,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="8"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="8"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="Width (mm)" Foreground="#999" FontSize="9"/>
                                            <TextBox Text="{Binding CurrentProject.PageSettings.CardWidthMm, UpdateSourceTrigger=LostFocus}" Padding="4" FontSize="11"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="Height (mm)" Foreground="#999" FontSize="9"/>
                                            <TextBox Text="{Binding CurrentProject.PageSettings.CardHeightMm, UpdateSourceTrigger=LostFocus}" Padding="4" FontSize="11"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="4">
                                            <TextBlock Text="Bleed (mm)" Foreground="#999" FontSize="9"/>
                                            <TextBox Text="{Binding CurrentProject.PageSettings.BleedWidthMm, UpdateSourceTrigger=LostFocus}" Padding="4" FontSize="11"/>
                                        </StackPanel>
                                    </Grid>

                                    <!-- Grid -->
                                    <TextBlock Text="GRID" Foreground="#0078D4" FontSize="10" FontWeight="Bold" Margin="0,12,0,6"/>
                                    <TextBlock Text="Leave blank to auto-fit" Foreground="#666" FontSize="9" Margin="0,0,0,4"/>
                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="8"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Foreground="#999" FontSize="9">
                                                <Run Text="Columns (auto: "/><Run Text="{Binding CurrentProject.PageSettings.AutoCardsPerRow, Mode=OneWay}"/><Run Text=")"/>
                                            </TextBlock>
                                            <TextBox Text="{Binding CurrentProject.PageSettings.ColumnsOverride, UpdateSourceTrigger=LostFocus, TargetNullValue=''}" Padding="4" FontSize="11"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="2">
                                            <TextBlock Foreground="#999" FontSize="9">
                                                <Run Text="Rows (auto: "/><Run Text="{Binding CurrentProject.PageSettings.AutoCardsPerColumn, Mode=OneWay}"/><Run Text=")"/>
                                            </TextBlock>
                                            <TextBox Text="{Binding CurrentProject.PageSettings.RowsOverride, UpdateSourceTrigger=LostFocus, TargetNullValue=''}" Padding="4" FontSize="11"/>
                                        </StackPanel>
                                    </Grid>

                                    <!-- Info -->
                                    <TextBlock Text="INFO" Foreground="#0078D4" FontSize="10" FontWeight="Bold" Margin="0,6,0,6"/>
                                    <TextBlock Foreground="#AAA" FontSize="11" Margin="0,2">
                                        <Run Text="Grid: "/><Run Text="{Binding CurrentProject.PageSettings.CardsPerRow, Mode=OneWay}"/>
                                        <Run Text=" x "/><Run Text="{Binding CurrentProject.PageSettings.CardsPerColumn, Mode=OneWay}"/>
                                        <Run Text="  |  "/><Run Text="{Binding CurrentProject.PageSettings.CardsPerPage, Mode=OneWay}"/><Run Text="/page"/>
                                    </TextBlock>
                                    <TextBlock Foreground="#AAA" FontSize="11" Margin="0,2">
                                        <Run Text="Page: "/><Run Text="{Binding CurrentProject.PageSettings.PageWidthMm, Mode=OneWay, StringFormat={}{0:0}}"/>
                                        <Run Text=" x "/><Run Text="{Binding CurrentProject.PageSettings.PageHeightMm, Mode=OneWay, StringFormat={}{0:0}}"/><Run Text=" mm"/>
                                    </TextBlock>
                                    <TextBlock Foreground="#AAA" FontSize="11" Margin="0,2">
                                        <Run Text="Cards: "/><Run Text="{Binding Cards.Count, Mode=OneWay}"/>
                                    </TextBlock>

                                </StackPanel>
                        </controls:SidebarSection>

                        <!-- ===== STORAGE SECTION ===== -->
                        <controls:SidebarSection x:Name="StorageSection" Header="Storage">
                                <StackPanel Margin="10,8">
                                    <TextBlock Text="STORAGE" Foreground="#0078D4" FontSize="10" FontWeight="Bold" Margin="0,0,0,6"/>
                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding CacheSizeText}" Foreground="#AAA" FontSize="11" VerticalAlignment="Center"/>
                                        <Button Grid.Column="1" Style="{StaticResource ToolBtn}" Command="{Binding ClearCacheCommand}"
                                                Content="Clear Cache" Padding="8,3" FontSize="10"/>
                                    </Grid>
                                </StackPanel>
                        </controls:SidebarSection>

                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- ==================== STATUS BAR (Row 5) ==================== -->
        <Border Grid.Row="5" Background="#007ACC" Padding="8,3"
                DataContext="{Binding ActiveProject.Inner}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding StatusText}" Foreground="White" FontSize="11"/>
                <TextBlock Grid.Column="1" Foreground="#CCE8FF" FontSize="11">
                    <Run Text="{Binding Cards.Count, Mode=OneWay}"/> cards  |  v<Run Text="{Binding AppVersion, Mode=OneTime}"/>
                </TextBlock>
            </Grid>
        </Border>

        <!-- ==================== SHELL LOADING OVERLAY ==================== -->
        <Border Grid.RowSpan="6" Background="#CC000000"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}, FallbackValue=Collapsed}">
            <Border Background="#2D2D30" CornerRadius="8" Padding="30,24"
                    HorizontalAlignment="Center" VerticalAlignment="Center" MinWidth="300"
                    BorderBrush="#0078D4" BorderThickness="1">
                <StackPanel HorizontalAlignment="Center">
                    <Ellipse Width="36" Height="36" StrokeThickness="4" Margin="0,0,0,16"
                             HorizontalAlignment="Center" RenderTransformOrigin="0.5,0.5">
                        <Ellipse.Stroke>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#0078D4" Offset="0"/>
                                <GradientStop Color="Transparent" Offset="1"/>
                            </LinearGradientBrush>
                        </Ellipse.Stroke>
                        <Ellipse.RenderTransform>
                            <RotateTransform/>
                        </Ellipse.RenderTransform>
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                        <DataTrigger.EnterActions>
                                            <BeginStoryboard x:Name="ShellSpinSB">
                                                <Storyboard RepeatBehavior="Forever">
                                                    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                                     From="0" To="360" Duration="0:0:0.8"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.EnterActions>
                                        <DataTrigger.ExitActions>
                                            <StopStoryboard BeginStoryboardName="ShellSpinSB"/>
                                        </DataTrigger.ExitActions>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock Text="{Binding LoadingMessage}" FontSize="14" FontWeight="SemiBold"
                               Foreground="White" HorizontalAlignment="Center"/>
                    <TextBlock Text="Please wait..." Foreground="#888" FontSize="11"
                               HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </Border>

        <!-- ==================== BUSY OVERLAY ==================== -->
        <Border Grid.RowSpan="6" Background="#CC000000"
                DataContext="{Binding ActiveProject.Inner}"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}, FallbackValue=Collapsed}">
            <Border Background="#2D2D30" CornerRadius="8" Padding="30,24"
                    HorizontalAlignment="Center" VerticalAlignment="Center" MinWidth="300"
                    BorderBrush="#0078D4" BorderThickness="1">
                <StackPanel HorizontalAlignment="Center">
                    <Ellipse Width="36" Height="36" StrokeThickness="4" Margin="0,0,0,16"
                             HorizontalAlignment="Center" RenderTransformOrigin="0.5,0.5">
                        <Ellipse.Stroke>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#0078D4" Offset="0"/>
                                <GradientStop Color="Transparent" Offset="1"/>
                            </LinearGradientBrush>
                        </Ellipse.Stroke>
                        <Ellipse.RenderTransform>
                            <RotateTransform/>
                        </Ellipse.RenderTransform>
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsBusy}" Value="True">
                                        <DataTrigger.EnterActions>
                                            <BeginStoryboard x:Name="SpinSB">
                                                <Storyboard RepeatBehavior="Forever">
                                                    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                                     From="0" To="360" Duration="0:0:0.8"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.EnterActions>
                                        <DataTrigger.ExitActions>
                                            <StopStoryboard BeginStoryboardName="SpinSB"/>
                                        </DataTrigger.ExitActions>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock Text="{Binding BusyMessage}" FontSize="14" FontWeight="SemiBold"
                               Foreground="White" HorizontalAlignment="Center"/>
                    <TextBlock Text="Please wait..." Foreground="#888" FontSize="11"
                               HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</Window>
